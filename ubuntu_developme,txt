# Install
sudo apt update
sudo apt install docker.io docker-compose nginx -y
sudo usermod -aG docker $USER

# Clone
git clone https://github.com/yourname/sparkly.git
cd sparkly

# Start
docker-compose up --build